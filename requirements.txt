# App + read API runtime (Streamlit Community Cloud installs this file).
# Streamlit Cloud uses Python 3.14 by default; every pin must ship cp314 wheels.
pandas==2.3.3
numpy==2.3.4
pyarrow==22.0.0
pydantic==2.13.4
streamlit==1.50.0
plotly==5.24.1
